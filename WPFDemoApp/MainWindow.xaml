<Window x:Class="WPFDemoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemoApp"
        mc:Ignorable="d"
        Title="DemoApp" Height="450" Width="800">
    <Grid Background="DarkGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Label to display "To Do List" -->
        <Label FontFamily="Times New Roman" Foreground="White" Content="To Do List" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="24" FontWeight="Bold"/>

        <!-- Grid to hold TextBox and Button -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox BorderBrush="Black" BorderThickness="1" x:Name="InputTextBox" Grid.Column="0" Margin="5,5,0,5" Text="Enter To Do Item" HorizontalAlignment="Left" Width="680"/>
            <Button Content="Add" Grid.Column="1" Width="75" Margin="5" HorizontalAlignment="Right" Click="AddButton_Click"/>
        </Grid>

        <!-- Border around the ItemsControl -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" Margin="10" Padding="10" Background="Gray">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding TextContentList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- TextBlock to display each item in the TextContentList -->
                            <TextBlock Foreground="White" Text="{Binding}" Margin="5" FontFamily="Trebuchet MS" FontSize="14"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
